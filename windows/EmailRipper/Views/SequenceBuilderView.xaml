<UserControl x:Class="EmailRipper.Views.SequenceBuilderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             Background="{StaticResource SurfaceBrush}">
  <Grid Margin="32,28,32,40">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <StackPanel Grid.Row="0" Margin="0,0,0,16">
      <TextBlock Text="Sequence builder" Style="{StaticResource H1}"/>
      <TextBlock Text="Define the email steps, delays, and conditional branches for a campaign." Foreground="{StaticResource MutedBrush}"/>
    </StackPanel>

    <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="Auto"/>
          <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <ComboBox Grid.Column="0"
                  ItemsSource="{Binding Campaigns}" SelectedItem="{Binding SelectedCampaign}"
                  DisplayMemberPath="Name" Padding="10,8"/>
        <Button Grid.Column="1" Content="Add step" Margin="10,0,0,0" Command="{Binding AddStepCommand}"/>
        <Button Grid.Column="2" Content="Save sequence" Style="{StaticResource PrimaryButton}" Margin="10,0,0,0" Command="{Binding SaveCommand}"/>
      </Grid>
    </Border>

    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
      <ItemsControl ItemsSource="{Binding Steps}">
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,12">
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="Auto"/>
                  <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Width="140" Margin="0,0,16,0">
                  <TextBlock Text="Step" Style="{StaticResource MetricLabel}"/>
                  <TextBlock Text="{Binding StepOrder}" FontSize="28" FontWeight="Bold" Foreground="{StaticResource AccentBrush}"/>
                  <TextBlock Text="Wait (days)" Style="{StaticResource MetricLabel}" Margin="0,12,0,0"/>
                  <TextBox Text="{Binding DelayDays}" Padding="10,8"/>
                  <TextBlock Text="Condition" Style="{StaticResource MetricLabel}" Margin="0,12,0,0"/>
                  <ComboBox SelectedValue="{Binding Condition}" SelectedValuePath="Content" Padding="10,8">
                    <ComboBoxItem Content="always"/>
                    <ComboBoxItem Content="no_reply"/>
                    <ComboBoxItem Content="no_open"/>
                  </ComboBox>
                </StackPanel>
                <StackPanel Grid.Column="1">
                  <TextBlock Text="Subject" Style="{StaticResource MetricLabel}"/>
                  <TextBox Text="{Binding Subject}" Padding="10,8" Margin="0,0,0,10"/>
                  <TextBlock Text="Body (HTML — variables like {{first_name}} are rendered)" Style="{StaticResource MetricLabel}"/>
                  <TextBox Text="{Binding BodyHtml}" Padding="10,10" MinHeight="160"
                           AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
                </StackPanel>
              </Grid>
            </Border>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </ScrollViewer>
  </Grid>
</UserControl>
