<Window x:Class="EmailRipper.Views.Dialogs.LeadImportDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        Title="Import leads from CSV"
        Width="820" Height="620"
        Background="{StaticResource SurfaceBrush}"
        WindowStartupLocation="CenterOwner">
  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <StackPanel Grid.Row="0" Margin="0,0,0,14">
      <TextBlock Text="Import leads" Style="{StaticResource H1}" FontSize="22"/>
      <TextBlock Text="Drop in a CSV. First row should be headers. Map columns to the required fields." Foreground="{StaticResource MutedBrush}"/>
    </StackPanel>

    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
      <Button Content="Choose CSV…" Style="{StaticResource PrimaryButton}" Click="OnChooseFile" Padding="14,10"/>
      <TextBlock x:Name="FileLabel" Text="No file selected" Foreground="{StaticResource MutedBrush}" VerticalAlignment="Center" Margin="14,0,0,0"/>
    </StackPanel>

    <DataGrid x:Name="PreviewGrid" Grid.Row="2" AutoGenerateColumns="True" HeadersVisibility="Column"
              Background="Transparent" Foreground="White" BorderThickness="0" RowBackground="#14FFFFFF"
              AlternatingRowBackground="#22FFFFFF" GridLinesVisibility="None" IsReadOnly="True"
              Visibility="Collapsed"/>

    <Grid Grid.Row="3" Margin="0,14,0,0" Visibility="Collapsed" x:Name="MapGrid">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <StackPanel Grid.Column="0">
        <TextBlock Text="Email *" Style="{StaticResource MetricLabel}"/>
        <ComboBox x:Name="EmailCol" Margin="0,4,4,0"/>
      </StackPanel>
      <StackPanel Grid.Column="1">
        <TextBlock Text="First name" Style="{StaticResource MetricLabel}"/>
        <ComboBox x:Name="FirstCol" Margin="4,4,4,0"/>
      </StackPanel>
      <StackPanel Grid.Column="2">
        <TextBlock Text="Last name" Style="{StaticResource MetricLabel}"/>
        <ComboBox x:Name="LastCol" Margin="4,4,4,0"/>
      </StackPanel>
      <StackPanel Grid.Column="3">
        <TextBlock Text="Company" Style="{StaticResource MetricLabel}"/>
        <ComboBox x:Name="CompanyCol" Margin="4,4,4,0"/>
      </StackPanel>
      <StackPanel Grid.Column="4">
        <TextBlock Text="Title" Style="{StaticResource MetricLabel}"/>
        <ComboBox x:Name="TitleCol" Margin="4,4,0,0"/>
      </StackPanel>
    </Grid>

    <Grid Grid.Row="4" Margin="0,18,0,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>
      <TextBlock x:Name="StatusText" Grid.Column="0" Foreground="{StaticResource MutedBrush}" VerticalAlignment="Center" TextWrapping="Wrap"/>
      <StackPanel Grid.Column="1" Orientation="Horizontal">
        <Button Content="Cancel" Padding="14,10" Margin="0,0,10,0" Click="OnCancel"/>
        <Button x:Name="ImportBtn" Style="{StaticResource PrimaryButton}" Content="Import" Click="OnImport" IsEnabled="False"/>
      </StackPanel>
    </Grid>
  </Grid>
</Window>
